# 观察台 · A-Share Watchdesk

一个**本地运行**的 A 股看板：多股对比 + 点击深挖（研报/龙虎榜/解禁/资金流/**多周期行情图：分时+K线蜡烛，可选 MA5/10/20/60/120/240**）+ 随时增删自选股 + **持仓盈亏跟踪** + **顶部大盘研判条** + **全市场两级选股** + **DeepSeek AI 每日推荐与买卖时机建议**（结果落盘缓存、带时间戳）+ **近1年新闻/政策库** + **私域笔记** + **交易规则库（价格行为体系，可增删改，注入 AI）** + **每个名词的新手解释**。**另含复盘自动化模块（`/review`）**：A 股短线情绪**每日复盘**——情绪硬指标（纯计算）+ **5 角色分析师、复盘裁判**（DeepSeek）+ 可发布文稿，收盘后自动生成。**三周期选股与自选股买卖点（观点账本）**：短/中/长三周期各 5 只、自选股每只给买点/卖点/时间窗、改口要有理由、结果自动贴回（页面「三周期」「买卖点」入口、接口 `/api/picks`）。**20 个模拟盘 agent 的舰队**：每个 agent 独立模拟账户、按真实行情自动交易并复盘记教训，页面 agent 区查看（站长专属）。

> 为什么是本地应用而非托管网页？深挖、加股票、调 AI 都需要**实时外部请求**，而托管型 Artifact 有严格 CSP 禁止一切外部请求。本地 Flask 后端代理即可。

## 本地安装（四步）

面向本地 macOS（`python3` 需 3.10+，`python3 --version` 确认）。

**第 1 步 · 装依赖**（一次）：
```bash
cd A-stock
python3 -m pip install -r requirements.txt      # 只依赖 flask
```

**第 2 步 · 填 DeepSeek key**——在项目根建 `.env`（已 gitignore，不进 git）：
```bash
echo 'DEEPSEEK_API_KEY=sk-你的key' > .env
```
不填也能跑，只是 AI 按钮自动隐藏（硬指标照常）。key 在 [platform.deepseek.com](https://platform.deepseek.com) 申请。可选变量：
```
DEEPSEEK_MODEL=deepseek-v4-pro       # 也可用 deepseek-v4-flash（更快更省）
DEEPSEEK_BASE_URL=https://api.deepseek.com
# BOCHAAI_API_KEY=sk-xxxx           # 可选：博查联网搜索，填了才启用（BOCHA_API_KEY 亦可）
```
改 key 后重启后端生效。

**第 3 步 · 建账号**（一次）——看板有登录页，个人数据按账号放在 `data/users/<账号>/`：
```bash
python3 astockctl.py adduser <你> --admin            # 首个管理员即 agent 舰队的站长
python3 deploy/migrate_to_multiuser.py <你>          # 老用户：把根目录旧数据迁到你名下（新装可跳过）
```

**第 4 步 · 启动**：
```bash
python3 app.py
```
浏览器开 **http://127.0.0.1:5000**，登录后左上角切「选股自动化 / 复盘自动化」。

三条注意：
- migrate 要在**第一次登录前**跑，否则登录会先建出空库、migrate 全部跳过（此时加 `--force` 重跑）。
- 本地不要设 `ASTOCK_ENV=production`：cookie 会带 Secure，http 下登不上。
- 用 `127.0.0.1` 别用 `localhost`：localhost 走 ::1，会撞 macOS AirPlay 的 5000 端口。

### 部署到服务器给几个人共用

登录、每人一份数据、AI 日预算、gunicorn + 独立调度进程、Tailscale 私网、阿里云一键脚本，全部在 `deploy/`，步骤看 `deploy/README-deploy.md`。服务器上 agent 默认不自动跑（`ASTOCK_AGENT_AUTO=0`）。

### 装好后全自动

- **复盘每天自动出**：只要 app 开着，**每个交易日 18:30 自动生成当日复盘**（等龙虎榜定稿；想改时刻，启动前设 `export REVIEW_AUTO_TIME=19:00`）。也可随时到 `/review` 点右上「生成今日复盘」手动出。
- **情绪周期开箱即用**：首次启动**后台自动回填近 3 个月**（约 15 秒），无需手动。

### 两个页面（路由）

- **`/` 选股看板**：多股对比 / 深挖 / 全市场选股 / 持仓盈亏 / AI 推荐与买卖建议。
- **`/review` 复盘**：A 股短线情绪复盘——涨停/连板/炸板/情绪周期/题材热点等**情绪硬指标**（纯计算）+ **DeepSeek 研判**（明日关注点）+ **可发布复盘文稿**，每交易日收盘后自动生成（批处理入口 `python -m review.run_daily`）。顶部可在两页间切换。

### 复盘模块 · 每日运行

- **看**：浏览器开 `/review`，点右上「生成今日复盘」手动生成，或由定时任务自动出。
- **命令行 / 定时器批处理**：
  ```bash
  python -m review.run_daily            # 复盘最近已收盘交易日（约 1~2 分钟）
  python -m review.run_daily 20260814   # 指定日期
  python -m review.run_daily --force    # 已生成也重跑
  python -m review.run_daily --no-ai    # 只算硬指标、不调 DeepSeek
  python -m review.backfill 3           # 情绪周期回填近 3 个月（首次跑一次即可）
  ```
- 结果落 `data/review/<date>.json`（已 gitignore），`/review` 自动读最新一份。

### 想关掉 app 也每天自动跑？（可选）

开着 app 就自动跑，对本地日常够用。若想**关着 app 也定时出复盘**，用 macOS launchd 挂定时任务跑批处理入口 `python3 -m review.run_daily`，装法照「自动抓取新闻库」一节的 plist（`launchctl bootstrap`），把命令换成复盘入口即可。

### 数据源提醒（换网络/换机都适用）

打板数据主要走**东财**，东财对部分 IP 有风控（偶发 `RemoteDisconnected` / 空返回，取数已内置限流+退避重试）。表现随网络/IP 变：
- **打板四池 / 龙虎榜取不到**：复盘核心数据缺，体检闸会拒绝生成（不出错误复盘）；换网络或稍后重试。
- **板块资金流取不到**：资金面分析师自动降级（5 角色减为 4），不影响主流程。

## 功能

| 区域 | 能力 |
|------|------|
| 自选股对比 | 现价、涨跌、PE、PB、**换手率**、年化波动、20日涨幅、区间位置条、主力20日净流入、1手成本、30日走势迷你图。点表头排序，点行深挖。 |
| 点击深挖 | 右侧抽屉 6 页：概览（指标+情景区间+**财报营收/净利同比**+**近期新闻**+走势）、**行情**（分时/5日 分时折线 + 近1月/近3月/近半年/近1年 **日K蜡烛 MA5/MA20+成交量**，鼠标逐点看 时间/OHLC·涨跌%·成交量·换手率；分时**交易时段每30s自动刷新**；下附箱形图价格分布）、研报（评级/EPS/PDF直链）、龙虎榜（席位TOP5+机构标注）、解禁风险、资金流（30日柱状图）。 |
| 大盘研判条 | 顶部常驻：上证/深成/创业板/沪深300/科创50 涨跌 + 两市成交额 + AI 一句话研判（强弱/风格/风险/攻防指导，可展开）；选股前先取大盘结论注入，让个股与大盘一致。 |
| 资产总览 | 总市值、总成本、**总盈亏（额+%）**、**当日盈亏（额+%）**、持仓数——大字卡片一目了然。 |
| 我的持仓 | 记录代码/股数/成本价/买入日，实时算市值、总盈亏、当日盈亏（红赚绿亏）。持久化 `portfolio.json`（按账号存个人目录，按投资画像隔离、多笔买入记录）。 |
| 自选推荐 | 把全部自选股指标 + 持仓喂给 DeepSeek，返回每只 buy/sell/hold/add/reduce/watch + 理由 + 风险 + 市场观点 + 持仓提醒。 |
| 全市场选股 | 从**全市场池**（全 A 名单，落 `data/universe.db`）**结合大盘、跨板块两级筛选**：**资金规模跟随你的投资画像**（现金本金，不再手选）、按其筛可负担标的、可下钻到某二级细分，给 6~10 只推荐 + **具体配置方案** + 板块简评。全市场池未就绪时用精选龙头池兜底（`universe.py`）。 |
| 三周期选股与自选股买卖点 | **短/中/长三周期**各给 5 只（短线 1 到 5 个交易日、中线 2 到 8 周、长线 3 到 12 个月）；自选股每只给**买点/卖点/止损/时间窗**。**观点账本**记下每次观点：改口必须点明触发事件（止损触发/目标达成/周期到期/新信息推翻），结果每日贴回。交易日 16:00 全量、09:05 定时刷新；页面「三周期」「买卖点」入口，接口 `/api/picks`，也可手动跑。 |
| 何时卖 | 单只持仓，DeepSeek 结合你的成本 + **波动史 + 换手率 + 财报增速 + 近期新闻 + 私域笔记 + 交易规则**，给出卖出/加仓/止损/止盈价位 + 基本面与政策面判断（含建议持有周期、依据规则）。 |
| 深度入场分析 | 深挖抽屉里对任一股（不必持有）：**是否入场 / 何时入场 / 怎么买 + 未来卖出策略预判**，严格遵循价格行为规则、结合你的资金。 |
| 溯源与依据 | entry/position 分析都带：**数据溯源条**（本次用了哪些源+新鲜度+条数，后端确定性）+ **逐条结论依据**（AI 引用信号名/规则ID、后端权威填值并校验：可核对 / 对不上）。让每条 AI 提议都可审计。 |
| 公司叙事 | 单股分析（entry/position）顶部「**做过 / 在做 / 要做** + 题材」卡（v4-flash 据 近期新闻+财报多期+公告+概念 合成，当日缓存，并注入主分析）；**每日推荐**每只附一句话叙事（零额外 LLM）。 |
| 全球宏观 | 每日从**外围实时数值**（WTI/布伦特原油·黄金·铜·道指/纳指/标普）+ 全球财经/地缘快讯，提炼「**外围要点 + 对A股板块的指向**」（v4-flash 合成、当日缓存），注入所有 AI 分析。例：油价涨 1.13%，利好石油开采/油服，利空航空/化工下游。 |
| 投资画像 | 本地多档建档（各自现金本金+风险偏好，**持仓每档独立**）；**总资产=现金+持仓，自动落 5 档**（微型/小型/中型/大型/超大），不同资产不同玩法（只数/仓位/标的/周期/风控），核心=**期望为正**（小搏赔率、大求稳），注入所有 AI 分析。切画像即换现金+持仓+档位。 |
| 模拟盘 | 模拟委托交易：多本金存档（可增删），按**真实行情 + A股规则**（整手/涨跌停/T+1/手续费）撮合下单，看持仓盈亏与委托记录。 |
| 模拟盘 agent 舰队 | **20 个 AI agent** 各带独立模拟账户，交易日**按真实行情自动研判、选股、决策、风控、挂限价单、复盘**，攒交易数据与教训库。页面 agent 区查看战绩与教训（GET 对登录用户开放）；舰队全站一套、归站长（首个管理员），写操作仅管理员/站长。本地开着 app 盘中自动跑，服务器默认不自动跑。 |
| 新闻 | 本地**滚动近 1 年**新闻/政策库（SQLite）：分板块/政策筛选，输代码看个股+拉更久历史；每次 AI 分析自动注入相关条目。 |
| 笔记 | 私域信息：打字/贴文本记录，**AI 一键结构化**（v4-flash 提摘要/关联股/板块/标签），永久保留、进 AI 上下文作你的私域认知。 |
| 规则 | 交易规则库（84 条：67 蒸馏自 Al Brooks 价格行为体系 + 17 条 **A股制度特性**）：分 13 类，可 **增删改 / 启用停用**；每条带**场景标签**（本金 小/中/大 × 周期 短线/波段/长线），选「当前场景」灵活启用；生效规则注入所有 AI 分析。 |
| AI 缓存 | 多类 AI 结果按 kind 落盘缓存（`data/ai_cache.json`，键带用户隔离）+ 时间戳：同输入未过期秒回、显示「分析于 X · 命中缓存」+「强制刷新」。 |
| 名词解释 | 每个名词都有新手向说明：表头 ⓘ 悬浮提示 + 顶部「名词解释」总表弹窗。 |
| 增删自选/持仓 | 输入代码即加；一键删除；筛选结果可一键 +自选。 |
| 自动刷新 | **默认开启，30 秒轮询**（对比表/持仓/大盘条；交易时段惰性刷新新闻）；可手动关闭。 |

约定：**红涨绿跌**（A 股惯例）。所有 AI 输出均标注「参考信号，不构成投资建议」。

### AI 分析的上下文（每次分析都会拼接）

每次 AI 分析（自选推荐 / 全市场选股 / 何时卖 / 深度入场 / 大盘研判）都会按序拼接以下上下文再判断：

- **1) 交易规则**：启用中的规则库（价格行为框架，见「规则」）置顶注入，AI 按此推理。
- **2) 本地新闻/政策库**：从近 1 年 SQLite 库检索相关条目（个股取该股近期、大盘取市场级+政策），带日期供按新鲜度加权。
- **3) 私域笔记**：与该股/板块相关的你的笔记，标注为「你的判断」、与客观数据区分。
- **A · 财经/政策快讯（免费，默认开）**：财联社快讯 + 东财 7×24 全球资讯，零 key。顶部芯片显示「新闻」。
- **B · 博查联网搜索（可选）**：在 `.env` 配置 `BOCHAAI_API_KEY`（或 `BOCHA_API_KEY`，两种变量名都认）后启用通用网页搜索（政策原文、行业动态等），针对个股/板块联网检索喂给 DeepSeek。顶部芯片显示「联网」。
  - 申请：[open.bochaai.com](https://open.bochaai.com/)（国内直连、人民币结算、DeepSeek 官方搜索，约 3.6 元/千次），拿 key 填入 `.env`，重启。
  - 未配置时自动跳过，只用 A，不影响任何功能。
  - **到期提醒**：启动时会探测 key 健康；若 key 失效/过期/余额不足，看板顶部弹出红色提醒并给出处理方式（芯片显示「联网」加警告）。也可点提醒里的「重新检测」。

## 数据源

用户面短版（完整口径与坑见 `CLAUDE.md`「数据源与坑」）：

| 数据 | 源 | 说明 |
|------|-----|------|
| 实时行情 / 估值 | 腾讯 `qt.gtimg.cn`（GBK） | 不封 IP |
| 大盘指数 / 两市成交额 | 腾讯 `qt.gtimg.cn` | 上证 + 深证综指成交额合计 |
| 涨跌家数 / 涨停跌停 / 行业冷热 | 东财 push2 clist + push2ex | 走 `em_get` 限流，失败降级 |
| 当日分时 | 腾讯 ifzq | 分时折线，交易时段自动刷新 |
| 日 K / 5 分钟 K 线 | 新浪 `getKLineData` | 前复权蜡烛；腾讯 hqkline 端点已失效 |
| 波动率 / 资金流 | 新浪 MoneyFlow | 含每日收盘价，一份数据两用 |
| 研报 / 龙虎榜 / 解禁 | 东财 reportapi + datacenter | 走 `em_get` 串行限流 |
| 全 A 名单 / 板块归属 | 新浪 hs_a（名单）+ 东财 slist（逐股回填板块） | 名单与板块统计共用一份数据 |
| 财报三表 | 新浪 | 营收、归母净利 + 同比 |
| 新闻 / 政策库 | 东财个股新闻 + 财联社快讯 + 东财 7×24 | 落 SQLite，滚动近 1 年 |
| AI 分析 | DeepSeek `deepseek-v4-pro`（推理，温度 0.15）/ `v4-flash`（公司叙事/宏观/笔记结构化/复盘分析师） | OpenAI 兼容 HTTP，密钥来自 `.env` |
| 复盘打板数据 | 东财 push2ex（涨停/炸板/跌停/昨涨停四池）+ 同花顺（题材串/涨停深史）+ 东财 datacenter（龙虎榜）+ 东财 clist（板块资金流） | `/review` 用；纯 urllib + 限流重试 |

## 文件结构

文件到职责的完整地图见 `CLAUDE.md`「文件地图」。数据布局：个人数据按账号在 `data/users/<uid>/`（自选股、持仓、画像、笔记、规则、模拟盘、agent、自选股买卖点），公共数据在 `data/`（新闻、全市场池、因子、模板、复盘、公共三周期、用量、账号）；库清单以 CLAUDE.md「数据文件」节为准。

## 自动抓取新闻库（launchd · 内置到 Mac）

新闻/政策库在你**打开看板时**会惰性刷新；想**关着看板也每天自动累积**，就把定时任务装进 macOS launchd。

> 仓库若在外置盘上——任务每次触发都会**先检测盘/脚本在不在**：在就抓，不在就**静默跳过**（不报错、不抓）。开机登录、插上盘、以及每天固定时间点，都各检测一次。

**触发时机**（每次先做盘存在性检测）：
- 开机 / 登录后跑一次（`RunAtLoad`）
- 插上外置盘（任何卷挂载）即跑一次（`StartOnMount`）
- 每天 `08:40 / 11:40 / 14:00 / 15:30 / 20:30`（`StartCalendarInterval`；非交易日仅 20:30 真抓，门控在 `fetch_news.py`）

**安装（一次性）**：
```bash
# 1) 若路径与默认不同，先改 launchd/com.astock.news.plist 里的：
#    - 仓库路径 REPO（plist 里的默认值是示例安装路径，改成你自己的安装路径）
#    - python 路径（默认 /usr/bin/python3；用 which python3 确认）
#    - 日志路径（默认 ~/Library/Logs/astock-news.log）
cp launchd/com.astock.news.plist ~/Library/LaunchAgents/   # 别加 sudo！否则 plist 归 root，重启后 launchd 可能拒载
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.astock.news.plist
```
> `bootstrap` 是新版 macOS 的官方命令（旧的 `launchctl load` 已废弃，且常报 `Load failed: 5: Input/output error`，别用）。
> `~/Library/LaunchAgents` 本就是你自己的目录，**无需 sudo**；若已误用 sudo，跑 `sudo chown "$(id -un):staff" ~/Library/LaunchAgents/com.astock.news.plist` 改回属主。

**验证 / 运维**：
```bash
launchctl print gui/$(id -u)/com.astock.news | head    # 已加载会打印任务详情
launchctl list | grep astock                            # 出现即已加载（这才是成功判据）
launchctl kickstart gui/$(id -u)/com.astock.news        # 立刻手动触发一次
python3 fetch_news.py                                    # 或直接命令行跑一次（盘在时）
python3 fetch_news.py --backfill                         # 手动回填 1–2 季度 + 研报
tail -f ~/Library/Logs/astock-news.log                  # 看抓取日志
```

**卸载**：
```bash
launchctl bootout gui/$(id -u)/com.astock.news
rm ~/Library/LaunchAgents/com.astock.news.plist
```

说明：
- plist 装在**内部盘** `~/Library/LaunchAgents/`，定义常驻；外置盘拔掉也不影响，盘不在时任务只是静默 no-op，**盘一挂上（或下个时间点）就自动开始抓**。
- Mac 在某时间点睡眠，该任务在下次唤醒时补跑一次。
- **交易日历按年自动抓取**（`news_store.py` 拉 `holiday-cn` 的 `{年份}.json` 并缓存到 db，一年只抓一次；抓不到就退化为纯工作日）——**无需手动维护**、自动跨年。

## 安全

- **密钥只存在 `.env`，源码不含任何 key**；`.env` 与 `data/`（公共数据、各账号个人数据、AI 缓存）均已 gitignore。
- 若 key 曾在聊天/截图等处明文暴露，建议到 DeepSeek 后台重置后写回 `.env`。

## 注意

- **交易时段**：盘中实时；非交易时段显示最近交易日数据。
- **AI 速度/成本**：`deepseek-v4-pro` 是推理模型，每日推荐约 20~40 秒；嫌慢可在 `.env` 换 `deepseek-v4-flash`。已给足 `max_tokens`（推理会占用额度）。
- **东财限流**：深挖偶发空数据是东财对当前 IP 的间歇风控，重试或换网络即可（页面显示「无数据」不崩）。
- **政策面**：AI 分析默认读财经/政策快讯（A，免费）；配置博查 key 后再叠加联网搜索（B）检索政策原文/行业动态。详见上文「AI 分析的上下文」。
- **行情图（分时+K线）**：分时/5日为分时折线（腾讯分时/新浪5分钟）；近1月/近3月/近半年/近1年为新浪日K（前复权）蜡烛图，含 MA5/MA20+成交量、悬停看当日 OHLC·成交量·换手率(近似=成交量÷流通股本)；下附箱形图展示区间收盘价四分位分布（图中星标为当前价）。分时在交易时段每 30s 自动刷新。**换手率**：实时换手率（腾讯）进对比表列+概览并注入 AI 研判；历史每日换手率在日K悬停给近似值。
- **仅供研究，不构成投资建议**；AI 与情景区间只描述客观信号与波动幅度，不保证收益。

## 免责声明 / Disclaimer

本项目仅用于技术学习与个人研究，**不构成任何投资建议**。所有行情、指标、情景区间及 AI 生成内容均基于公开数据的客观计算或模型输出，可能存在错误或延迟，不保证准确性、完整性或收益。据此进行的任何投资决策及其后果由使用者自行承担。数据来源于各第三方公开接口，其可用性与合规性由相应提供方负责。

## License

[MIT](LICENSE) © 2026 Gresham429
