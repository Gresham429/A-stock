# Backlog / 待办

> 已确认方向的功能。✅=已上线，⏳=待做，🔒=**卡在数据源、代码已就位**。

---

## 🔒 卡在数据源的两项（2026-07-16）

**这两项不是没做，是现有数据源给不了。代码路径都已就位，拿到源即可接上。
用户要求：找到可用源时提醒他加。**

### 🔒 1. 资金分量 `net20` 无法回测

**卡点**：`ds.sina_metrics()` 的 `series` 只返回 **30 天**（源头 `MoneyFlow.ssl_qsfx_zjlrqs`
即使传 `num=260` 也只给 30 天）。因子回测需要 ≥600 天历史才有统计意义。

**后果**：`_pa_score` 四个分量里，价格类三项（`vol`/`cum20`/`range_pos`）已用
162,014 个样本验过方向，**唯独资金项没验** → 现在它**不参与打分、仅展示**。

**需要什么源**：任一能给 **≥600 天历史每日主力净流入** 的接口。候选（均未验证）：
- 东财 `push2his`（**本机 IP 已被封**，`clist` 也封，见 CLAUDE.md 数据源表）
- 通达信 `mootdx`（本地协议，但**海外可用性存疑**——`company_profile` 当初就是为此避开它）
- 其他第三方（同花顺 / 聚宽 / Tushare，多需 token）

**接上后要做**：把 `factor_lab.FACTORS` 加上 `net20`，重跑 `backtest()`，
用 `direction()` 定它的方向，再让 `_pa_score` 纳入它。改动量很小——框架已通。

### 🔒 2. 宏观 digest 缺 美元指数 / VIX

**卡点**：`ds.global_markets()` 用的新浪外盘**没有这两个**（`hf_DX` 实测返回空，VIX 无对应代码）。

**后果**：`macro_digest` 现在有 油价(WTI/布伦特)/黄金/铜/美股三大指数，
但**缺两个最关键的风险指标**——美元指数（人民币汇率与外资流向的锚）、
VIX（全球风险偏好温度计）。外围风险切换时 AI 抓不到。

**需要什么源**：能给美元指数与 VIX **实时数值**的 HTTP 接口（要海外可用）。候选（均未验证）：
- 东财外盘、腾讯外盘
- Yahoo Finance（`^VIX` / `DX-Y.NYB`，海外可用但国内可能不通）

**接上后要做**：`ds.global_markets()` 加两个条目即可 —— `macro_digest` 会自动带上，
提示词已写明「**数值优先于新闻措辞**」。无需改提示词。

---

## ✅ 全球宏观/地缘 digest 层（方案 B）— 已上线 2026-07-15

`llm.macro_digest`(v4-flash) 据 东财7×24全球 + 财联社 提炼 `{points, sector_map, bias}`；
`app._macro_block()` 前置注入 5 个 AI；`ai_cache` kind=`macro` 当日 6h 缓存。
传导由 AI 推理（不硬编映射表）。

## ✅ 外围市场数值指标（方案 C）— 已上线 2026-07-15

`ds.global_markets()`（新浪外盘 HTTP，海外可用）：**WTI/布伦特原油 · 黄金 · 铜 · 道指/纳指/标普500**，
喂进 `macro_digest`，提示词要求**数值优先于新闻措辞**。
实测：「WTI涨1.13%/布伦特涨1.45%→利好石油开采/油服，利空航空/化工下游」「黄金跌0.73%→压制黄金板块」，并能识别内外背离。
**已知缺口**：该源无**美元指数**(`hf_DX`)与 **VIX**（实测返回空）——想要得另找源（东财/腾讯外盘），价值中等，暂不做。

## ✅ 本金可配 + 按总资产分级玩法 template — 已上线 2026-07-14

多档本地画像（现金+风险偏好）、每档独立持仓、总资产落 5 档、玩法 template 注入 5 个 AI。
见 `plan/2026-07-14-capital-profiles-templates-design.md`。

## 可选后续（未承诺）

- **美元指数 / VIX**：换源补齐外围数值（见上「已知缺口」）。
- **溯源(provenance) 推广**：现仅 entry/position；daily/screen/market 也可加。
- **template 文案 UI 编辑**：现 5 档玩法是代码常量，只读展示；可做成可编辑。
